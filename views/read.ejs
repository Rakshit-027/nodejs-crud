<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/styles/index.css">
    <title>Document</title>
</head>
<body>
   <div class="h-full w-full bg-zinc-900 p-10 text-white flex wrap">
        <div>
            <h1>Users</h1>
            <div class="flex flex-wrap gap-4 ">
            <% users.forEach(user => { %>
                <div class="bg-zinc-700 h-72 w-56 p-3 mt-3 rounded-lg">
                    <div class="bg-zinc-500 h-50 w-50 items-center rounded-lg">
                        <img class="h-50 w-50 rounded-lg" src="<%= user.image%>" alt="">
                    </div>
                    <p class="text-l text-white mt-2"><%= user.name %></p>
                    <p class="text-xs text-slate-400"><%= user.email %></p>
                    <div class="flex gap-2 mt-2">
                        <a class="text-xs text-blue-500" href="/edit/<%=user._id%>">Edit</a>
                        <a class="text-xs text-red-500" href="/Delete/<%= user._id%>">Delete</a>
                    </div>
                </div>
            <% }) %>

        </div>
        </div>
    </div>

        

           </div>


        </div>
       
    </div>
</body>
</html>